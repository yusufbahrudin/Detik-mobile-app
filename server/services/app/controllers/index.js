const { Auth } = require("./auth")
const { News } = require("./news")
const { CategoryController } = require("./category")
const { UserController } = require("./user")

module.exports = { Auth, News, CategoryController, UserController }
